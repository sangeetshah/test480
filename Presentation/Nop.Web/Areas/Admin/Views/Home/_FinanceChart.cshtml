@model ProfileModel

<div class="col-md-4 finance-pie-chart" style="display: none; max-width: 30% !important;">
    <div class="card card-info">
        <div class="card-header clearfix" style="background-color: #ffffff;">
            <div class="float-left" style="color: #000000;">Finance</div>
        </div>
        <div class="card-body">
            <canvas id="finance-chart" height="300"></canvas>
        </div>
    </div>
</div>

<script>
    var financeCtx = document.getElementById('finance-chart').getContext('2d');
    var financeChart;

    $("#generate-chart").click(function () {
        var applicantId = $("#ApplicantId").val();

        $.ajax({
            url: '@Url.Action("GetFinanceCompletionPercentage", "Finance", new { area = "Admin" })',
            type: 'POST',
            data: {
                applicantId: applicantId,
                __RequestVerificationToken: $('input[name="__RequestVerificationToken"]').val()
            },
            success: function (data) {
                $('.finance-pie-chart').css("display", "block");

                if (financeChart) {
                    financeChart.destroy();
                }

                financeChart = new Chart(financeCtx, {
                    type: 'pie',
                    data: {
                        labels: ['Filled', 'Empty'],
                        datasets: [{
                            data: [data.Filled, data.Empty],
                            backgroundColor: ['#4CAF50', '#f44336']
                        }]
                    },
                    options: {
                        responsive: true,
                        plugins: {
                            legend: {
                                position: 'bottom',
                            }
                        }
                    }
                });
            }
        });
    });
</script>