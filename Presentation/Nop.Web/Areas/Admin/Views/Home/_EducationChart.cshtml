@model ProfileModel

<div class="col-md-4 education-pie-chart" style="display: none; max-width: 30% !important;">
    <div class="card card-info">
        <div class="card-header clearfix" style="background-color: #ffffff;">
            <div class="float-left" style="color: #000000;">Education</div>
        </div>
        <div class="card-body">
            <canvas id="education-chart" height="300"></canvas>
        </div>
    </div>
</div>

<script>
    var educationCtx = document.getElementById('education-chart').getContext('2d');
    var educationChart;

    $("#generate-chart").click(function () {
        var applicantId = $("#ApplicantId").val();

        $.ajax({
            url: '@Url.Action("GetEducationCompletionPercentage", "Education", new { area = "Admin" })',
            type: 'POST',
            data: {
                applicantId: applicantId,
                __RequestVerificationToken: $('input[name="__RequestVerificationToken"]').val()
            },
            success: function (data) {
                $('.education-pie-chart').css("display", "block");

                if (educationChart) {
                    educationChart.destroy();
                }

                educationChart = new Chart(educationCtx, {
                    type: 'pie',
                    data: {
                        labels: ['Filled', 'Empty'],
                        datasets: [{
                            data: [data.Filled, data.Empty],
                            backgroundColor: ['#4CAF50', '#f44336']
                        }]
                    },
                    options: {
                        responsive: true,
                        plugins: {
                            legend: {
                                position: 'bottom',
                            }
                        }
                    }
                });
            }
        });
    });
</script>